import { Avatar, Card, CardActions, CardContent, CardMedia, CardHeader, IconButton, Typography } from "@mui/material"

export default function Post() {
    return (
        <Card>
            <CardHeader avatar={<Avatar>A</Avatar>} />
        </Card>
    )
}